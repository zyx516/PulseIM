server:
  port: 8083
spring:
  application:
    name: social-service
  datasource:
    url: ${SOCIAL_DB_URL:jdbc:mysql://localhost:3306/pulseim_social?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true}
    username: ${PULSEIM_MYSQL_USER:root}
    password: ${PULSEIM_MYSQL_PASSWORD:pulseim-dev}
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    open-in-view: false
